+//
|欢迎使用 Dialogue，这是一个贴近纯文本的可视化编程工具
|熟练使用之后，你可以获得类似于传统文本编程的编写体验
+// 在Dialogue中，代码文件由一系列块组成，每一个块都代表一个语句
+@输出 "hello, world"
+@读入 "输入一个数字" > n
+%set i = 5
+%while i > 0
    +%set n *= 2
    +%set i = i - 1
+%if n > 435
    +&then
        +@输出 a
    +&else
        +@输出 b